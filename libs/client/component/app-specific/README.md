# client-component-app-specific

This library was generated with [Nx](https://nx.dev).

## Running unit tests

Run `nx test client-component-app-specific` to execute the unit tests via [Jest](https://jestjs.io).
