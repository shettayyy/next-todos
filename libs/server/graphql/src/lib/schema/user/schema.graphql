extend type Query {
  user(id: ID!): User
}

type User {
  id: ID!
  fullName: String!
  isAdmin: Boolean!
}

input CreateUserInput {
  fullName: String!
  isAdmin: Boolean!
}

extend type Mutation {
  createUser(input: CreateUserInput!): User
}
